1.정적함수와 동적함수
클래스에서 static 키워드가 붙은 함수와 
붙지 않은 함수의 메모리 할당에 따른 차이를 서술하세요.

static 키워드가 붙은 함수는 인스턴스를 생성하지않고도 
기능을 온전히 사용할수있어서 메모리사용을 줄일수있다.
없는경우 새로운 인스턴스를 새로 생성하여야 하기 때문에 인스턴스가 낭비된다.

2. 유니티에서 뛰어가는 중인지 아닌지 판단하려면 
어떤 자료형을 써야하고 왜 써야하는지 상세하게 적으시오

예를들어 미리 public bool isrun= false; 형식을 설정해두고
{뛰어가는 중}이라는 값의 참과 거짓에 따라 컨트롤러에 할당된 (예:캐릭터, 이펙트, 총, 소리 등)을 설정할수있다.

3. 유니티에서 이펙트의 자료형은 무엇인가?
Particle System

4. C$에서 재귀 함수는 자기 자신 함수를 호출하는 함수이다.
 다음을 코딩 하세요
실행예) 2의 몇승을 입력 할까요?
입력 : 8
출력 256 재귀함수로 구현하세요.

 static int Mypower(int num, int cnt)
 {
     if (cnt == 0)
     {
         return 1;
     }
     return num * Mypower(num, --cnt);

 }
 static void Main()
 {
     Console.Write("2의 몇승을 입력할까요? : ");
     int y = int.Parse(Console.ReadLine());
     System.Console.WriteLine(Mypower(2, y));
 }

5. 유니티에서 프리팹을 왜쓰는지 사용 쓰임새를 서술하세요.
미리 프리팹으로 설정하여 복사하거나 하면 
각각 스크립대입 혹은 설정변경등의 번거로움을 한번에 해결할 수 있다.

6. 유니티에서 프리팹을 동적 생성(인스턴스) 하는 함수에 대해 적고
전달 인자의 값의 순서와 내용에 대해 적으세요
instantiate    object  transform.position    quaternion.identyty
                 물체 자체           위치                         회전정도

